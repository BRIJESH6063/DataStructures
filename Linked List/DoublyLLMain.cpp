#include<bits/stdc++.h>
#include"DoublyLL.h"
using namespace std ;

int main()
{
    Node* head = NULL ; Node* tail = NULL ; 
    TakeInputTail(head, tail) ;
    PrintList(head) ;
    PrintList(tail) ;
    return 0 ;
}